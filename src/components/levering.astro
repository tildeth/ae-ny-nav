---
import "../style/style.css";
---
<div class="boxen">
<p class="send">Hvem skal vi sende din gavehilsen til?</p>
<section class="box">
    <img class="blomst" src="/img/hortensia.jpeg" alt="hortensia">
<div class="skriv">
    <p>For- og efternavn</p>
    <input type="text" name="name" placeholder="Fx. Alberte Andersen">

    <p>Leveringsadresse</p>
    <input type="text" name="name" placeholder="Fx. Guldbjergsgade 125, 3500 København V">
  
    <p>Leveringsdato</p>
    <div class="delivery-date">
        <label for="today">
      <input type="radio" id="today" name="delivery-date" value="today">
        <img class="dato" src="/img/idag.png" alt="idag">
   I dag</label>

   <label for="tomorrow">
      <input type="radio" id="tomorrow" name="delivery-date" value="tomorrow">
      <img class="dato" src="/img/imorgen.png" alt="imorgen">
     I morgen</label>
 
     <label for="other-date">
      <input type="radio" id="other-date" name="delivery-date" value="other">
      <img class="dato" src="/img/anden.png" alt="anden">
      Anden dato</label>
  </div>
</div>
</div>

<style>

.boxen {
    padding: 19px;
    max-width: 80%;
    margin-left: 7%;
}

.send {
    color: var(--pink);
}

.box {
  position: relative;
}

.box .send {
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  padding: 10px;
  position: relative; 
}

.box img {
        width: 100%;
        height: 50vh;
        object-fit: cover;
        z-index: 1;
        opacity: 0.4;
    }

    .skriv {
        width: 80%;
  position: absolute;
  top: 50%; /* Placer skriv-containeren i midten */
  left: 80%;
  transform: translate(-90%, -50%); /* Centrer skriv-containeren */
  padding: 20px;
  border-radius: 8px;
  z-index: 2; /* Sørg for, at teksten er foran billedet */
}

.box p {

  margin-top: 0;
  color: var(--brown);
}

.box input[type="text"],
.box input[type="radio"] {
  width: 100%;
  padding: 10px;
  margin-bottom: 10px;
  background-color: rgba(255, 255, 255, 0.7);
  border-radius: 7px;
}

.delivery-date input[type="radio"] {
    display: none;

}

.delivery-date {
  display: flex; /* Brug flexbox */
  justify-content: center; /* Centrer indholdet vandret */
}

.delivery-date label {
    display: flex;
    justify-content: center;
  color: var(--pink);
  background-color: rgba(255, 255, 255, 0.7); 
  padding: 10px 15px;
  margin-right: 10px;
  border-radius: 7px;
  cursor: pointer;
  padding: 20px;
  width: 80%;
  white-space: nowrap;
}

.delivery-date input[type="radio"]:checked + label {

    background-color: rgba(255, 255, 255, 0.7); 
  color: var(--pink);
}

.delivery-date input[type="radio"] + label:focus {
  border-color: var(--brown); /* Ændre kantfarven ved fokus */
}

.skriv img {
  width: 20px; /* Justér størrelsen efter behov */
  height: auto;
  max-width: 100%; /* Sørg for, at billedet ikke overstiger containerens bredde */
  margin-right: 10px;
  margin-top: -1px;
}

input:focus {
  outline: none;
}
</style>